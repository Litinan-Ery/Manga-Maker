# 分镜修复用户用例与验收记录

## UC-100-03E：特写和画外人物不强制完整外观

第66、79、80、95–100页多次重绘仍出现跨格人物或附加头部。核对发送请求发现：用户改成物件/手部特写后，编译器仍在该格追加原来的完整角色身份，并将全部原角色加入全页外观参考。先补PRD，再用物件特写预览复现不支持出镜角色覆盖（422）。现可选择本格原角色的子集或空集，完整角色资料只从实际选择中收集，选项进入冻结哈希，界面修改会撤销旧确认。未知格、陌生/重复角色被拒绝，旧计划原哈希仍可读。相关后端8项、前端用例、ruff、mypy及build通过。真实修订、逐页审阅和中文排字已完成，最终100页全部通过本次生产验收。特写修订有效抑制跨格附加人物；模型仍可能偏离姿态等细节，差异保留在逐页记录。

## UC-100-03D：格框识别失败时旁白仍须紧凑

第78、81页的框线检测未通过，校准函数提前返回，连旁白框尺寸也未收紧。已先在PRD记录边界，再用出血图像复现。修复后保持原格框与格外自由文字不变，仅在既有文字安全区内收紧旁白；保留全文和字号。相关23项测试、ruff与mypy通过；两页实际重新排字并视觉通过，文字完整且不再用大片白框遮画面。

## UC-CTX-00：新增检查点迁移后的健康与启动基线

全量回归发现三项测试仍期待schema33，而既有检查点迁移已增加到34。按PRD第25节的恢复验收，启动、健康检查及重复迁移必须报告实际34；此次只更新过期基线断言，不修改迁移或业务数据。原始失败证据已保存，更新后单独复测。

## UC-100-04A：100页跨章节导出（已完成真实验收）

作为长篇漫画作者，我希望将三个章节的35/35/30页一次导出为完整100页，并能从工程包恢复继续编辑。

已定位限制：`ExportPreflightRequest` 和 `CreateExportRequest` 仅接受单个 `chapter_id`，页面列表最多64项；服务查询也仅限一个章节。现有入口不能表达本次100页全书选择。

验收须覆盖：100页顺序及四种格式；缺章/缺页/乱序/重复/跨工程拒绝；旧单章接口兼容；页面版本与哈希冻结；恢复工程后文字可编辑。离线用例与真实100页成品分别留证，完成前不得将此项标为通过。

离线阶段已通过：新增全书预检与导出接口，导出中心增加全书范围。100页35/35/30隔离编号图验证PNG/PDF/CBZ/工程包数量及顺序、跨章页码、固定哈希、缺章/缺页/乱序/重复/未知页面拒绝。相关后端8项、前端2项、类型检查和ruff通过，Mock图像请求增量0。真实100页现已完成四格式导出：100个PNG、100页PDF、100图CBZ和工程包；CRC、固定版本、页序及哈希通过。解锁后的秘密扫描0命中。工程包在独立空应用目录恢复为35/35/30页，100个渲染哈希一致，300个文字图层可编辑，修改旁白后生成新版且旧版不变。详细证据见[100页验收报告](canticle-100-acceptance.md)。

日期：2026-09-06。基线：main / a122c84。工作区既有《沙王》24页改动保留。

规则：按 UC-01 → UC-02 → UC-03 → UC-04 执行；本项通过后再继续下一项。测试过程使用隔离临时数据库，既有工程、素材与凭证不作为测试可写数据。

## UC-01：批准的版式成为正式页面

作为漫画作者，我在版式工作台批准不等宽、由右向左阅读的页面后，希望正式页面逐格使用该版式，文字落在我保留的区域；再次修改并批准版式时，能使用已有素材创建新版页面。

验收：
- 格框、画布、阅读方向、格顺序和裁切焦点与批准快照一致。
- 文字安全区从格内归一化坐标转换为页面像素；多个文字图层不得重叠占用同一区域，放不下明确报错。
- 再次拼页保留旧版本、已有素材与编辑后的文字；相同版式重复请求不生成额外版本。
- 缺失、未批准或已失效的版式不会回退默认模板。
- 拼页全过程图像服务请求增量为零。

状态：通过。

- 旧代码复现：UC-01 API 两个用例失败，分别返回旧的 left_to_right 页面及错误的 201 成功。
- 修复后：自定义格框/文字区/版本保留/审批门禁/嵌套三格/文字区容量用例通过。
- 回归：页面、渲染、导出、重绘、架构边界共 47 项通过；页面编辑器前端用例 1 项通过；全后端 mypy（192 文件）和前端 typecheck 通过。
- 外部图像调用：测试 Mock 计数证明拼页增量为 0；未使用真实 NovelAI。

## UC-02：视觉约束可追踪到 NovelAI 请求

作为漫画作者，我把某格从中景改为特写、移动焦点或留白后，希望最终发送的提示明确包含这些变化。单格自然语言描述中的标点和段落应保留，前置文本模型漏写画面描述时也不能让已批准的分镜内容消失。

验收：固定 seed 和尺寸，分别修改景别/焦点/裁切安全区/文字区时，最终生成请求的对应语义发生变化；控制用例中仅审批元数据变化不改画面语义。新旧 Prompt 快照都可读，旧哈希不会因新增可选字段而失效；完整保留固定角色 Tags 和角色坐标隔离。

状态：通过。

- 旧代码复现：新增 7 个定点用例中 5 项失败（四类视觉约束、完整自然语言）；元数据控制与旧哈希读取通过。
- 修复后：新编译器、NovelAI 映射、API 保存/预览、旧契约兼容、执行冻结与版式绑定共 67 项回归通过；前端描述编辑/未保存禁批用例通过，typecheck 与后端 mypy 通过。
- 画面描述独立于 Tags 保存；文本模型未提供时回退已批准分镜描述。构图要求由批准版式确定性生成，固定角色 Tags 与角色坐标继续独立传递。
- 外部图像调用：离线 Mock；本项验证请求语义与执行一致性，不声称模型像素级遵循几何坐标。

## UC-03：一页一次的 V5 多格生成

作为漫画作者，我选择 V5 整页多格模式后，希望检查一份包含版式、阅读顺序、逐格事件、角色连续性与文字策略的整页 Prompt，确认后每页只生成一张图，并把它作为不可变页面素材继续审阅和导出。

验收：
- 模式、模型、文字策略、每页 Prompt 和负面提示在生成前可见；修改任意输入后旧审批不可复用。
- 使用已批准的分镜、版式与固定角色资料编译，普通页仍须满足 3–6 格，画面按版式顺序描述。不能用角色位置框冒充分格区域。
- 支持本地文字与模型文字两种策略；后者把 Text: 放在末尾，不含禁止文字/格框/网点的冲突提示，并关闭会附加 no text 的质量预设。
- 整页生成的单位是页，有限页数、单候选、成本上限、凭证读取与请求冻结可核验；请求重试不自动重复扣费，产物保存为新版本。
- 页面使用模型已画好的格框，整图只绘制一次；本地文字策略保留可编辑图层，模型文字策略不再叠加重复文字。
- 原有逐格流程及历史页面继续可读、可导出。

状态：通过。

- 旧代码复现：整页预览用户用例返回 405，应用不存在整页生成入口。
- 修复后：独立整页预览/冻结/一次生成/成图检查/采用与历史可用；两种文字策略、重复执行、超时不重放、版式过期、四类导出及工程恢复后继续编辑通过。
- 回归：相关 API、NovelAI 映射、契约、迁移、架构、导出与健康检查 70 项通过；前端编辑、预览、确认、成图采用等 3 项通过；mypy 197 文件及前端 build 通过。
- 外部调用：本项仍为隔离 Mock。真实模型格框遵循能力在 UC-04 单独记录。

## UC-04：多格漫画的端到端验收

作为漫画作者，我希望普通页样例包含 3–6 格，不把一页一幅插图当作漫画分镜成功。用原创的《雨夜的钥匙》验证人物进门、视线、手部插入镜头和反应的连续性。

验收：
- 用三格与四格页，包含不等宽格框、右到左顺序、全景/特写/手部插入镜头和文字安全区；检查批准版式、最终请求、图片与导出是否对应。
- 逐格拼页验证确定性几何；整页模式验证每页 1 次调用、所有格都有明确叙述、角色 Tags 连续、模型文字位于 Text: 末尾。
- 保存可重复执行的样例与验收报告，记录 seed、请求哈希、页格数量、调用计数和导出文件。
- 本地 Mock 验证产品链路与几何。真实 NovelAI 请求和成图视觉检查独立记录，不能用 Mock 的彩色块代替模型质量结论。
- 既有《沙王》一页一格案例明确归类为通页插画测试。

状态：本地链路及真实三格/四格成图与本地日语排字通过；精确模型文字仍未通过，已标为实验能力。

- 新增三格/四格两页端到端用例，两种模式均完成审批、生成、采用和工程包/PNG/PDF/CBZ 导出；逐格 Mock 图像调用为 7 次，整页为 2 次，导出调用增量为 0。
- 补充复现并修复：V5 逐格仍传 Human Focus UC 预设、逐格与整页相互绕过串行门禁、未知结果误报配置错误。对应失败用例已转为通过；不确定结果仍禁止自动重放。
- 使用不对称三色图验证整图像素只绘制一次，不重复裁入各格、不附加多余格框。两种文字策略均完成恢复工程后继续编辑。
- 浏览器实际完成：切换模式 → 选择模型文字 → 预览 → 勾选生成 → 待审阅成图 → 勾选审阅 → 采用为 v2。确认前生成/采用按钮禁用，旧 v1 保留；模型文字不能添加重复文字层。
- 全后端：343 项通过；全前端：23 个文件、49 项通过；ruff、mypy（198 源文件）、TypeScript 与生产 build 通过。最后的样例/文档调整另复跑相关 6 项后端测试及全前端，均通过。唯一测试提示是第三方 Starlette TestClient 的 httpx 弃用提醒。
- 可重复脚本：`uv run python -m scripts.run_storyboard_manga_acceptance`。本次成功产物：[报告与导出](../.manga-maker/storyboard-acceptance/20260905T193800.095884Z/README.md)，包含原文、分镜、最终 Prompt、请求哈希、调用数和四类导出。目录名采用 UTC，执行本地日期为 2026-09-06。
- 上述离线阶段真实 NovelAI 请求数为 0。最初因凭证库锁定而保留的两份预览仍在 [pending-review.json](../.manga-maker/storyboard-ui-real-20260905T192947/pending-review.json)，它们没有发出外部请求。用户解锁常用工作区后，将验收样例作为独立项目导入，并创建新的预览执行；每份预览仍只生成一次，实际凭证仅由应用读取。
- 真实续验总计 7 次图像请求、7 次订阅/额度核验，包含 5 次模型文字诊断和 2 张最终无字底图。最终采用三格与四格底图，在本地校订日语对白、音效与文字位置；四种格式导出、哈希、两页 PDF/CBZ、隔离工程恢复及继续编辑均通过，排字/导出/恢复的图像调用增量为 0。
- 最终全量回归：346 项后端、49 项前端通过；ruff、mypy（198 源文件）、TypeScript 与生产 build 通过。完整请求/图片哈希、失败候选、最终成品与费用资格边界见 [真实验收报告](../.manga-maker/storyboard-real-final/README.md)。

## 本轮结论与未包含范围

### UC-04A：真实三格页不能被拆成四条横格

2026-09-06 真实 V5 Full 首次请求（seed 9201）完成，PNG SHA-256 为 `18c5da39b34861a96aeab1e34a692dd5cf5720483ab5a40a8ef8444edff590a5`。要求上排左右两格、下排一格，实际生成四条横格，钥匙动作被拆成两格；此图不采用、不计为成功。文件：`.manga-maker/storyboard-real-final/page-1-original.png`。

已定位的编译缺口：最终请求只含格框百分比，没有直接表达“上排左右两格、下排通栏”；大量默认中心/全幅保护区重复提示掩盖视觉叙述，且具体服装未被编译。模型像素偏差的因果仍需同 seed 成图比较，不能仅凭代码推断修复成功。

新增验收：三格与四格页均明确行列、逐格位置及已批准服装；固定 Tags 和非默认构图约束保留；模型文字仍在末尾。先运行编译回归，再同 seed 生成三格页视觉复验，通过后继续四格页。状态：本样例布局拓扑通过，不代表像素级版式保证。

同 seed 9201 第二次生成已恢复三格及“上排两格、下排通栏”拓扑（SHA-256 `07e1d71af78941302bccddc90747d33ac8a9bdc7abd0c65053e357fcf64fae2f`），已批准服装进入最终 Prompt；编译和整页 API 共 11 项测试通过。该结果证明本样例布局改善，不代表精确几何控制或普遍成功率。

### UC-04B：对白不得跨格互换或附带格号

第二次真实图的上排对白互换，且格间出现多余编号，所以仍不采用。编译器用“text entries 1 through 1”关联末尾文字，并多次提及 Panel 编号；改为方位标题、逐格内联实际字符串、末尾保留 `Text:`，负向加入格号/页码。官方文字渲染文档允许在自然语言中重复字符串增强关联。新增用例检查逐格归属、方位标题、编号禁令、末尾列表与两种文字策略。状态：文字绑定编译通过；模型精确排字未通过，产品默认推荐本地文字完成准确交付。

内联文字后的三格图通过布局/必需对白归属/编号检查，但额外添加音效；四格图（seed 9202，SHA-256 `f6629f246adc7dd0d1b5b44c9d6ef3f310f423c87521320e5f1ee9e6e00a2a76`）布局与镜头正确，却把“手紙…”和“…わかった。”放入相邻格。因此模型文字的精确归属尚未通过，追加全页方位文字对应表验证，若仍失败则按本地文字策略完成准确交付。

全页文字对应表改善首句归属，末句仍串格（SHA-256 `7680f984f2b2c4b9dc9bbab3b9fd28c42bdac0dae7596a085c03e7610423d252`）。模型文字保留为实验能力，不能声称精确文字验收通过。改用本地文字，首张无字三格底图与本地排字已视觉通过，文字框根据真实成图调整。

### UC-04C：本地对白不带说话人前缀

本地文字验收发现图层自动生成“林夏: ただいま。”，把说话人打印在气泡里，且多句对白被合并。要求对白内容等于原文，说话人进入独立 `speaker` 字段，各句独立图层；历史作者编辑保留。新增预览→Mock 生成→采用的回归用例，先复现后修复。状态：通过；实际四格页采用后各对白图层与原文一致，说话人单独保存。

UC-01～03 的代码和用例门禁已通过；UC-04 的真实多格成图、可编辑本地文字、导出与恢复已完成。模型文字不能稳定精确归属，仍为实验能力；整页几何与人物细节仍需审阅，严格几何优先使用逐格生成与本地排版。
上述验收结束时尚未提交或发布代码，原工作区已有的《沙王》24 页改动保留。相关实现现已纳入 v0.2.2 代码提交，合并前后端 411 项、前端 57 项及离线 CLI 复验见[发布回归记录](context-recovery-acceptance.md#v022-release-validation)；整体 v0.3 仍未完成。
