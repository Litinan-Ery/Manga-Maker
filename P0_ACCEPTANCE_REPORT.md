# Manga Maker P0 分层验收报告

| 项目 | 结果 |
|---|---|
| 日期 | 2026-08-09 |
| 范围 | 单章 TXT → 分镜/设定 → Mock 逐格生成 → 页面编辑/reroll/恢复 → 工程包/PNG/PDF/CBZ |
| 离线 Mock 结论 | 通过 |
| 真实 NovelAI 付费 smoke | 未执行；等待用户针对实际调用单独授权 |
| 代表性授权章节真实生产 | 未执行；不得以合成测试章节代替 |

## 已验证结果

- 后端共 96 项测试通过；`ruff` 和严格 `mypy` 通过。
- 前端共 15 项测试通过；TypeScript 检查和 Vite 生产构建通过。
- 自有合成章节完成来源锚点、结构化分镜、角色/风格审批、有界 Mock 生成、本地改字、单格 reroll、素材与页面历史恢复、四格式导出及工程包 dry-run。
- 该闭环只发生 2 次本地 `MockNovelAIClient` 图像调用；真实 NovelAI 请求为 0。
- 重启演练证明：发送状态未知的任务进入 `needs_review`，不自动领取或重放；中断导出不发布，项目/素材半成品保留在恢复边界。
- 注入磁盘耗尽证明：供应商响应后的本地素材提交失败归类为 `LOCAL_STORAGE_FULL`，队列进入人工审阅且不重放；导出失败不修改旧成功版本。
- 解锁凭证库后，导出会用当前凭证字节扫描普通文件和 ZIP 内部条目。注入合成凭证后导出失败关闭，响应不回显凭证，旧成功导出哈希不变。
- 默认 HTTP access log 已关闭；恢复摘要和安全诊断不包含绝对工作区路径、小说正文、完整提示词或凭证。

## 验收命令

```bash
uv run ruff check backend tests
uv run mypy backend
uv run pytest
pnpm --dir frontend test
pnpm --dir frontend build
git diff --check
```

## 尚未完成且继续受门禁约束

1. 使用用户账户执行一次低成本 NovelAI 图片生成 smoke，复核当日官方 Swagger、模型字段、费用和条款。
2. 用一个用户明确授权的代表性中文章节完成真实全章生产，记录调用数、耗时、失败、可验证实际成本或估算边界，并进行角色一致性人工抽检。
3. 以上两项都需要新的、明确针对真实付费调用的用户授权；“继续开发”不自动授权外部付费请求。

因此，本报告只证明 P0 产品流程的离线实现与安全故障边界，不宣称 NovelAI 真实服务已经接通或生产质量已经验收。
